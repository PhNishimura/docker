
version: "3.8"

services:
  backend:
    image: concord_backend
    ports:
      - "3001:3001"
    volumes:
      - backend-db:/app/database.sqlite

  frontend:
    image: concord_frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  backend-db:
